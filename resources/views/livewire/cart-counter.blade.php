<span>
    @if($count > 0)
        <span class="inline-flex items-center justify-center w-5 h-5 rounded-full bg-brand-500 text-white text-xs font-bold">
            {{ $count }}
        </span>
    @endif
</span>
